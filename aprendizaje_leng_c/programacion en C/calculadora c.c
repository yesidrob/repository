#include <stdio.h>

 int mane() {

    int numero1= printf("introduzca el primer numero:") ;
     scanf("%f",&numero1);

    int numero2= printf("introduzca el segundo numero:") ;
     scanf("%f",&numero2);

sumar (a, b)  ;
printf("La suma de"a"+"b"es igual"a+b) ;

restar (a, b) ;
printf("La resta de "a" - "b" es igual a "a-b) ;

multiplicar (a, b) ;
printf("La multiplicacion de "a" x "b" es igual a "a*b) ;

dividir (a, b);
    if b == 0: printf("No se ouede dividir entre cero");
    else printf("La dividir de "a" / "b" es igual a "a/b);
while true:
     printf("""Indique la operacion a realizar:
            1) Sumar
            2) Restar
            3) Multiplicar
            4) dividir
            5) Salir de la calculadora
             """
            ) ;
return 0; 
}